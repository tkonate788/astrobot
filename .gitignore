# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm

# Build artifacts
dist/
build/
*.log

# Environment / secrets
.env
.env.local
.env.production
.deploy.env
docker/.env
backend/.env
admin/backend/.env
*.key
*.pem

# Claude / IDE / OS
.claude/
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo

# Quarantine (old versions pending deletion decision — kept local only)
archive/

# Personal data (kept local only)
deliverables/cv/

# Backups
*.tar.gz
*.bak
*.backup

# Python (build/deploy scripts)
__pycache__/
*.pyc
.python-version

# Local DB backups (user data — never publish)
backups/
